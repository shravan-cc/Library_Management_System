import { describe } from 'vitest';
import { BookInteractor } from './book.interactor';

describe('Tests block which contains test for input validation', () => {
  let bookInteractor;
  beforeEach(() => {
    bookInteractor = new BookInteractor();
  });
  test('Tests for recieving proper inputs from the user', () => {});
});
